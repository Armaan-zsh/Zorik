<Window x:Class="LauncherApp.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:LauncherApp.Utils"
        Title="Launcher" Height="220" Width="640" WindowStyle="None" AllowsTransparency="True" Background="Transparent" Topmost="True" ShowInTaskbar="False" WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <local:FileIconConverter x:Key="FileIconConverter" />
        <Style TargetType="TextBox">
            <Setter Property="FontFamily" Value="JetBrains Mono, Consolas, 'Courier New'" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
        </Style>
    </Window.Resources>

    <Grid Margin="12" Background="Transparent">
        <Border Background="#FF0F0F0F" CornerRadius="8" Padding="10" Opacity="0.98" HorizontalAlignment="Center" VerticalAlignment="Center" MaxWidth="720" MinWidth="420">
            <StackPanel>
                <TextBox x:Name="SearchBox" FontSize="14" Height="30" Padding="6" MaxWidth="680" HorizontalAlignment="Center" PreviewKeyDown="SearchBox_PreviewKeyDown" />

                <ListBox x:Name="ResultsList" Margin="0,6,0,0" Background="Transparent" Foreground="White" BorderThickness="0" ItemsSource="{Binding Results}" SelectedItem="{Binding SelectedResult, Mode=TwoWay}" MaxHeight="160" KeyboardNavigation.DirectionalNavigation="Cycle">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" Margin="6">
                                <Image Width="28" Height="28" Margin="4" Source="{Binding Path=Path, Converter={StaticResource FileIconConverter}}" />
                                <TextBlock Text="{Binding Title}" VerticalAlignment="Center" FontFamily="JetBrains Mono, Consolas" FontSize="14" Margin="8,0,0,0" />
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>

                <TextBlock x:Name="HotkeyStatusText" Text="" Foreground="#FF888888" FontSize="12" Margin="6,6,0,0" TextWrapping="Wrap" />
            </StackPanel>
        </Border>
    </Grid>
</Window>
